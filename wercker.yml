box: debian
build:
    steps:
        - me6iaton/hugo-build@1.0.1
        - me6iaton/hugo-build:
            version: "0.14"
            # bin: bin/hugo-linux
            theme: angels-ladder
            config: config.toml
            flags: --disableSitemap=true
